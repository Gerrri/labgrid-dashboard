# Environment variables for staging profile
# Use with: docker compose --env-file .env.staging --profile staging up

# Disable mock mode to use real Labgrid Coordinator and DUT connections
MOCK_MODE=false
