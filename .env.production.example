# Production Environment Configuration Template
# Copy this file to .env.production and customize for your environment

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Labgrid Coordinator Connection
# Format: ws://host:port/ws or host:port (protocol will be added automatically)
# Example: ws://coordinator:20408/ws or coordinator:20408
COORDINATOR_URL=ws://coordinator:20408/ws

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# Allowed origins for API access (comma-separated)
# Set this to match your dashboard's public URL(s)
# Examples:
#   - Single origin: http://localhost
#   - Multiple origins: http://localhost,https://dashboard.example.com
#   - Wildcard (not recommended for production): *
CORS_ORIGINS=http://localhost

# =============================================================================
# COORDINATOR SETTINGS
# =============================================================================

# Coordinator realm name
COORDINATOR_REALM=realm1

# Connection timeout in seconds
COORDINATOR_TIMEOUT=30

# =============================================================================
# COMMAND EXECUTION SETTINGS
# =============================================================================

# Command execution timeout in seconds
LABGRID_COMMAND_TIMEOUT=30

# Polling interval for target status updates (seconds)
LABGRID_POLL_INTERVAL_SECONDS=5

# =============================================================================
# CONFIGURATION FILES
# =============================================================================

# Path to commands configuration file (inside container)
COMMANDS_FILE=/app/commands.yaml

# Path to target presets configuration file (inside container)
PRESETS_FILE=/app/target_presets.json

# =============================================================================
# DEBUGGING
# =============================================================================

# Enable debug logging (true or false)
# Set to true for verbose logging, false for production
DEBUG=false

# =============================================================================
# ADVANCED SETTINGS (Optional)
# =============================================================================

# External WebSocket URL (only needed if behind reverse proxy)
# Default: /api/ws (relative URL)
# Set this if the external WebSocket URL is different from the internal URL
# Example: wss://dashboard.example.com/api/ws
# WS_URL_EXTERNAL=/api/ws

# =============================================================================
# NOTES
# =============================================================================

# 1. Frontend environment variables (VITE_*) are NOT needed in production
#    because nginx handles all API proxying with relative URLs
#
# 2. The entrypoint.sh script automatically generates env-config.js
#    with the correct API_URL and WS_URL for the frontend
#
# 3. Volume mounts are configured in docker-compose.prod.yml, not here
#
# 4. Always restart the container after changing environment variables:
#    docker restart labgrid-dashboard
